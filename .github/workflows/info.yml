name: Info

on:
  workflow_dispatch

jobs:
  info:
    runs-on: ubuntu-latest
    steps:
      - run: echo '${{ toJSON(github) }}'
      - run: echo '${{ toJSON(inputs) }}'
      - run: echo "$GITHUB_REF_NAME"